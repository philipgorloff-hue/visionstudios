import ContactClient from '@/components/ContactClient';

export const metadata = {
  title: 'Contact — Vision Studios',
  description: "Get in touch with Vision Studios. Motion design, 3D web, interactive experiences — let's build something together.",
};

export default function ContactPage() {
  return <ContactClient />;
}
